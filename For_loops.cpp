#include <iostream>

int main(){

    // for loop is a loop that will execute a block of code that will specified amount of times
        //? within the for loop can create up to 3 indexes 
    for(int i = 10; i <= 0; i-=1 ){
        std::cout << i << '\n'; 
    }
    std::cout << "Happy New year! \n";

    return 0;
}